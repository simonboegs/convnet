import torch
import torch.nn as nn

class Model1:
    def __init__(self):
        super(Model1, self).__init__()
        self.layers = nn.Sequential(
                )
    
    def forward(self, x):
        return self.layers(x)

